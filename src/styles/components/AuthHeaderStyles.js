import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    cabezeraContenedor: {
        justifyContent: 'center',
        alignItems: 'center',
        marginTop: 20
    },
    logo: {
        width: 100,
        height: 100,
        borderRadius: 50
    },
    cabezeraTitulo: {
        fontSize: 30,
        color: '#C8102E',
        marginTop: 10
    },
});
